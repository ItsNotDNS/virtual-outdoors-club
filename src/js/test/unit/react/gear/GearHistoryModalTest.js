import React from "react";
import GearHistoryModal from "react/gear/GearHistoryModal";
import { expect } from "chai";
import { shallow } from "enzyme";
import sinon from "sinon";

const emptyFunc = () => {};

describe("GearHistoryModal Tests", () => {
    /*
    it("Renders null if there is no gear", () => {
        const modal = shallow(
            <GearHistoryModal
                onClose={emptyFunc}
            />
        )

        modal.instance()
    })
    */
});